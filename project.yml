name: Evensong

options:
  bundleIdPrefix: com.evensong
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "26.0"

settings:
  base:
    SWIFT_VERSION: 6.0
    MARKETING_VERSION: 1.0.0
    CURRENT_PROJECT_VERSION: 1
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    DEVELOPMENT_TEAM: 4HFQ952344
    CODE_SIGN_STYLE: Automatic

packages:
  EvensongCore:
    path: Packages/EvensongCore

targets:
  Evensong:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: App/Evensong
        excludes:
          - Resources/**
      - path: App/Evensong/Resources
        buildPhase: resources
    dependencies:
      - package: EvensongCore
        product: EvensongCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.evensong.app
        PRODUCT_NAME: Evensong
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
    info:
      path: App/Evensong/Info.plist
      properties:
        CFBundleDisplayName: Evensong
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        ITSAppUsesNonExemptEncryption: false
        LSApplicationCategoryType: public.app-category.music
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        UIBackgroundModes:
          - audio
        UILaunchScreen: {}
        NSLocalNetworkUsageDescription: Evensong connects to your local NAS or computer to stream and cache your media.
        NSBonjourServices:
          - _smb._tcp
          - _webdav._tcp
          - _ftp._tcp
        NSSupportsLiveActivities: false
        UIUserInterfaceStyle: Dark
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight

  EvensongTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: App/EvensongTests
    dependencies:
      - target: Evensong
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.evensong.tests
        GENERATE_INFOPLIST_FILE: YES

schemes:
  Evensong:
    build:
      targets:
        Evensong: all
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - EvensongTests
